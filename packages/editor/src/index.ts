/**
 * @d3-polytree/editor — create and modify polytree diagrams.
 *
 * B3 skeleton: extends the base Viewer; modelling, palette and the properties/
 * side-tabs panels are filled in by subsequent B3/B6 PRs.
 */
import { Viewer, type ViewerOptions } from '@d3-polytree/viewer';

export type EditorOptions = ViewerOptions;

export class Editor extends Viewer {}

export default Editor;
